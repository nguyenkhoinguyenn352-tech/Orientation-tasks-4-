A-phase

The A-phase advances the simulation clock to the time of the next event and removes that event from the event list.

B-events

B-events generate new future events. For example, when a customer arrives, another arrival event may be scheduled for the future.

C-events

C-events perform the actual processing of the event. For example, a customer joins a queue, starts receiving service, or leaves the system.
